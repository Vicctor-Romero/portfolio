# Copy this file to `.env` and fill in the values.
# In production, set the same variables in Netlify:
#   Site settings → Environment variables.

# Resend API key — https://resend.com/api-keys  (required for the contact form)
RESEND_API_KEY=

# Sender address. Must be on a domain verified in Resend.
# (Real value lives only in Netlify env / your local .env — not committed.)
CONTACT_FROM="Your Name <hello@yourdomain.com>"

# Where contact-form messages are delivered.
CONTACT_TO=you@example.com
